# Static SPA serving with history fallback: deep links like /anything
# resolve to index.html so client-side routing never 404s.
server {
    listen 5173;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }
}
